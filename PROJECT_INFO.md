# Lbast Bot — обзор проекта

Автоматизация браузерной игры **lbast.ru** на Playwright. Бот играет за персонажа
**Tsunami**: выполняет ежедневные квесты, фармит боями, собирает ресурсные под-задания
и восстанавливает HP, а Telegram-бот-менеджер управляет запуском/остановкой и шлёт
оповещения (нападения, почта).

> Документ описывает состояние на 2026-08-07. При крупных изменениях — обновлять.

---

## 1. Из чего состоит проект

| Файл | Назначение |
|------|-----------|
| `daily_quests_piraty.js` | **Главный сценарий** (~4600 строк). Весь игровой цикл: квесты, фарм (Блейк или гоблины — см. `FARM_TARGET`), восстановление, рыбалка, реакция на PvP. |
| `manager_bot.js` | Telegram-менеджер. `spawn`-ит выбранный сценарий (передаёт `FARM_TARGET` через env), ведёт логи, шлёт алерты, автоперерыв 18ч работы / 6ч отдыха. |
| `yantar_v_gore.js` | Отдельный фарм-сценарий (кнопка «Янтарная гора»). `bleyk.js` — источник, из которого маршрут/бой Блейка перенесён в главный сценарий (standalone-кнопки нет). `gobliny_v_shahtah.js` (чистый фарм) и `volki_v_lesu.js` — из меню убраны (гоблины теперь через «Квесты + Гоблины»). |
| `daily_quests_piraty.state.json` | Персистентное состояние дейликов (что уже сделано сегодня), переживает перезапуск. |
| `update_pvp_stats_v2.js`, `check_page.js`, `open_browser.js`, `manual_login_and_check.js` | Вспомогательные/отладочные утилиты. |
| `logs/` | Логи запусков (`ГГГГ-ММ-ДД_ЧЧ-ММ-СС__<Сценарий>.log`), скриншоты нападений. |
| `chrome-profile/` | Персистентный профиль Chromium (сессия/куки — вход в игру сохраняется). |
| `backups/` | Ручные бэкапы главного сценария. |

**Стек:** Node.js + Playwright (`chromium.launchPersistentContext`, headful) +
`node-telegram-bot-api`/собственный HTTPS-клиент к Telegram. Платформа — Windows, PowerShell.

---

## 2. Как это запускается

```
manager_bot.js  ──spawn──►  node daily_quests_piraty.js
      │                              │
   Telegram                    Playwright ──► lbast.ru
```

- Менеджер читает `.env`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, опц. `TELEGRAM_SOCKS_PROXY`.
- Управление — reply-клавиатурой в Telegram. Кнопки сценариев:
  **Квесты + Блейки**, **Квесты + Гоблины** (оба — главный скрипт, отличается только
  `FARM_TARGET`), **Янтарная гора**. Плюс: «Запустить сейчас», «Статус», «Стоп»,
  «Старт 6ч» (отложенный старт на 6 часов).
- **Без «Длительности»:** запуск работает до ручного «Стоп» или до авто-перерыва.
- **Автоперерыв (рандомный):** после **17–18ч** непрерывной работы сценарий убивается на
  **6–6.6ч**, потом авто-перезапуск. Значения выбираются заново каждый цикл, чтобы
  расписание не было машинно-ровным.
- Правки кода подхватываются **только при перезапуске** сценария.

---

## 3. Главный цикл (`doScenario`)

Каждый «цикл» (по умолчанию сон 14–21 мин между ними) выполняет по порядку:

1. **Чтение статов** с `location.php` → `parseStats` вытаскивает `hpCurrent/hpMax`,
   `cooldown`/`reserveMinutes` (резерв в минутах), `Q` (доступные квесты), `D` (дейлики).
2. **Почта** — `handleUnreadMailIfAny` (пересылает текст письма в Telegram).
3. **Критичный минус HP** (`< -1500`) → `runLastHouseRecovery` (Последний дом), `return`.
4. **Минус HP** (`< 0`) → «Кулак хаоса», `return`.
5. **Виноград** (раз в 8ч) → полить виноградник.
6. **Статуя славы** (раз в 12–14ч) → активировать.
7. **Рыбалка** (между квестами, если не на острове Блейка) → попытка поймать карася.
8. **Ежедневные квесты** (`runDailyQuests`) — если `Q > 0`.
9. **Эксклюзивные квесты** (Харчевня/Штольни): пока один в процессе — остальное не
   запускается, только он.
10. **Рыбий глаз** — награда (после 10 боёв) и/или бой (интервал 25 мин, 10/день).
11. **Повторная проверка Q** после Рыбьего глаза.
12. **Восстановление в Стоунгарде** — если HP в «мёртвой зоне» (0…1800) и мы **не** на
    острове Блейка.
13. **Фарм Блейка** — если целевых квестов нет и HP ≥ 1800: цикл боёв, оставаясь на
    острове (уходим только при минусе HP или ради квеста).

### Пороги HP (главные константы)

| Константа | Значение | Смысл |
|-----------|----------|-------|
| `LAST_HOUSE_HP_THRESHOLD` | `-1500` | ниже — полноценное восстановление в Последнем доме |
| (Кулак хаоса) | `< 0` | быстрое лечение прыжком |
| `BLAKE_MIN_HP` | `1800` | порог, чтобы биться с Блейком **и** выход из восстановления; ниже (но ≥0) — «мёртвая зона» → Стоунгард |

---

## 4. Квесты и под-задания

**Целевые Q-квесты** (`runDailyQuests`, порядок = приоритет): Харчевня, Дерево жизни,
Штольни, Камни Драбаса, Кузница Рума, Еда для рыбака, Грабим корованы, Варьете,
Смерть ростовщика, Ордо экзекуторс (два задания).

- **Искать травы** — сбор трав (Дикий пустолист / Трава арайя / Кустарник травии / Семя
  винограда) через Q-меню: клик по названию травы → «К месту выполнения» → ожидание 7 сек →
  «В пути» → «Искать травы» → мини-игра-сапёр 4×4 («Заросли», `playHerbBoard`/`solveHerbBoard`
  в `daily_quests_piraty.js`). Клетки — ссылки `gamekl=1..16`; открытая цифра = число шипов
  среди соседей; решатель перебирает совместимые комбинации (диапазон шипов 3–4) и кликает
  клетку с наименьшей вероятностью шипа. Провал (шип) = ~4ч кулдаун, успех (трава) = ~12ч;
  доступность видна прямо в тексте Q-меню, отдельный таймер в состоянии бота не нужен.

- **Харчевня / Штольни** — «эксклюзивные»: пока идёт один, другие Q-квесты не берутся;
  есть окно фокуса 30 мин и бэкоффы при ошибках/таймауте.
- **Штольни** требует резерв ≥ 20 мин, HP ≥ 2000 (иначе шаги не гриндятся).
- **Еда для рыбака** — резерв ≥ 1 мин, **Грабим корованы** — ≥ 8 мин (правила Паши, 26.09.2026).
- **Варьете** — резерв ≥ 7 мин (правило Паши от 26.09.2026): сцена почти целиком диалоговая,
  переходов по карте мало. До этого стояло 15, и квест сутками отсеивался гейтом.
- **Дерево жизни** — до 3 раз/день, интервал 4ч.
- **Камни Драбаса** — до 2 раз/день, интервал 4ч.
- **Ордо экзекуторс** (`runOrdoQuestsIfAvailable`) — меню Q только объявляет доступность,
  само задание берётся в Башне Ордо Экзекуторс (Стоунгард → Южные ворота → юг → восток).
  Занимает слот «Текущее задание», поэтому не берётся при активной Харчевне/Штольнях.
  Требует резерв ≥ 15 мин и HP ≥ 2000, за цикл выполняется не больше одного задания.
  «Уничтожить главаря банды» (конь `q2001_1`, Медальон бандита, запрет 60 мин) и
  «Уничтожить банду» (конь `q2001_2`, Костяная цепь бандита, запрет 10 мин). **Предметы
  задания НЕ сдаём** — копим в инвентаре (Паша, 24.09.2026); слот «Текущее задание» игра
  освобождает сама после победы, так что второй заход в башню не нужен. Маршруты перенесены
  из проекта AI-персонажа (ветка `claude/lbast-character-registration-gc7bey`,
  `ai_char/lib/quests_story.js`).
  С минусовой моралью в гильдию не пускают — это ловится по тексту «не рады».
- **Рыбный ресторан** (`guides/fish_restaurant.js`) — ежедневный квест форта «Жженый лист»
  (Гретхис, тёща Кумуса). Особенность: маршрут не один, у квеста 27 наград и у каждой свой путь
  кликов. Что доступно сегодня, видно только в `[Журнал наград]` — а он открывается лишь на
  экранах `go=2`/`go=3` УЖЕ ВЗЯТОГО задания, после сдачи ссылка пропадает. Поэтому ветку
  выбирает сам прогон: читаем журнал → ищем награду в таблице `BRANCHES` → идём её шагами.
  Каждый уникальный предмет выдаётся раз в 30 дней, иначе за ветку дают только дины.
  Приоритет (`PRIORITY`): резервные эликсиры → шмот → боевые амулеты → расходники.
  Ветки 4 и 26 уводят с карты (Волчьи острова, Дорожный крест) и помечены `manual`, ветка 12
  ведёт на заведомо проигрышный бой (`danger`) — в цикле они не берутся. Требует резерв ≥ 15 мин
  и HP ≥ 1300. Порог — это «когда начинать», а не «хватит на весь прогон»: за один заход резерв
  уходит с 23 до −4, и раннер пережидает отдых прямо в сцене. Гайд: Dikaya, `zhg_web.php?st_id=224300` и
  `st_id=225290`.

**Периодические под-задания** (по таймеру, вне Q-меню):
Рыбий глаз (10 боёв/день + награда), Виноград (8ч), Статуя славы (12–14ч),
Рыбалка (6 уловов/день, кулдаун 2 мин), **Утренний скриншот** (1 раз/день, максимально рано —
первый же цикл после смены дня; маршрут Стоунгард → Центральная площадь → Уличный зазывала,
скриншот шлётся в Telegram через `MORNING_SCREENSHOT:<base64>` в stdout, разбирается
`maybeSendMorningScreenshot` в `manager_bot.js`, как `ATTACK_ALERT`/`MAIL_MESSAGE`).

**Резерв перед боем на Янтарной горе**: ≥ 8 минут (`AMBER_MIN_RESERVE_MINUTES`, правило Паши от
26.09.2026). Это правило про бой, а не про выезд: у Блейка противник в хижине под рукой, а на горе
к каждому бою надо заново спускаться в шахту (шахта → дальше → налево → дальше → В бой), и каждый
переход стоит резерва — с меньшим запасом бот встаёт на полпути. У Блейка и гоблинов порог
прежний («резерв > 0»). Если до 8 не хватает, цикл ждёт ровно добор (`amber_reserve_gate`).

**Цель фарма** (`FARM_TARGET`, задаётся менеджером): `blake` (по умолчанию), `goblins`,
`yantar` (Янтарная гора). Блейк и Янтарная гора — острова за платной лодкой (10 дин),
поэтому у них одинаковые пороги HP и правило «с острова ради филлеров не уходим»;
к гоблинам ведёт бесплатный портал, оттуда бот уходит отдыхать в Стоунгард.
Маршрут Янтарной горы: Амулет → Девтаун → ремесленный район → восток → Пристань →
лодка до острова Старого башмака → Далее → восток → север ×3 → к горе ×2 → шахта →
дальше → налево → дальше → В бой.

Механики, вынесенные в память ассистента: `fish_eye_mechanic`, `fishing_mechanic`,
`blake_farming_mechanic`.

---

## 5. Состояние и «дневник»

- `getDayKeyNow()` даёт ключ `ГГГГ-ММ-ДД` (**локальная** дата) — по нему сбрасываются
  дневные счётчики.
- `restoreDailyQuestState()` (при старте) и `persistDailyQuestState()` (после каждого
  значимого действия) синхронизируют in-memory-переменные с `…state.json`.
- Персистятся: таймеры/счётчики Дерева жизни, Рыбьего глаза, Драбаса, Винограда, Статуи,
  рыбалки, утреннего скриншота и флаги «сделано сегодня» для Харчевни/Штольни/Кузницы Рума/Еды
  рыбака/Корованов.

---

## 6. Безопасность и живучесть

- **PvP-нападение** (`handleIncomingAttackIfAny`): распознаёт входящий бой, делает
  случайные блок/удар по зонам, шлёт в Telegram алерт со скриншотом
  (`ATTACK_ALERT:<base64>`), после боя проверяет глубокий минус HP → Последний дом.
- **Почта** — пересылается (`MAIL_MESSAGE:<base64>`).
- **Устойчивость шагов:** `performStep` (клик + проверка успеха по смене URL/текста/
  появлению след. шага, с ретраями), `clickByTexts` → `…Loose` → `…Forced`
  (нарастающая настойчивость для нестандартных кнопок).
- **Мягкие не-ошибки:** ожидаемые «не готово» (кулдаун арены, «квестзапрет», нехватка
  резерва) не бросают исключение, а перепланируют внутренний таймер и выходят чисто —
  чтобы не срабатывал общий бэкофф `Retry after N min`.
- **Общий бэкофф:** любое непойманное исключение цикла → сон 14–21 мин.
- **Пауза (кнопка в Telegram, файл `pause.flag`)** — кооперативная, а не только в начале цикла:
  `throwIfPausedByManager()` стоит в `performStep` (то есть в каждом шаге любого маршрута),
  в `goToLocationAndReadStats`, в цикле станций Последнего дома, между боями фарма и между
  шагами `.steps`-раннера. Ошибка помечена `scenarioPaused` и проходит насквозь через
  `runQuestStepSafe`/`runNonQQuestSafe`/`runGuide` — главный цикл гасит её **без** снимка,
  `recoverToCity` и возврата на `location.php`, чтобы не сбить экран пользователю.
  Бои намеренно не прерываются: `fightLoop` идёт через `clickByTexts`, где проверки нет.

---

## 7. Известные слабые места и идеи по улучшению

Ничего из этого не ломает бота прямо сейчас, но стоит держать в голове:

1. **Мёртвый код.** Не используются: `shouldUseFishByStats`, переменная
   `nonQQuestFailedThisCycle` (присваивается, но не читается), no-op `saveSnapshot`.
   → Удалить, чтобы не путало. (Кластер гоблинов и `shouldFightByStats` снова в деле —
   через режим `FARM_TARGET=goblins`.)
2. **Дублирующийся блок** «Exclusive quest in progress» в `doScenario` (два одинаковых
   `if`-а подряд из-за закомментированного UGO между ними). → Схлопнуть в один.
3. **PvP-сабмит.** После выбора зон код жмёт «Ударить», но реальная кнопка отправки в
   PvP бывает «Бить» — в логах видно `Не найдено … "Ударить"`. Защита best-effort, но
   удар может не отправляться. → Проверить и добавить «Бить» в набор сабмита.
4. **Сброс дня по локальной дате.** Если игровой сброс дейликов не в локальную полночь,
   счётчики могут разъезжаться с игрой на несколько часов. → Свериться со временем
   сброса на сервере и при необходимости привязать `getDayKeyNow` к нему.
5. **`tavernTakenToday`/`shtolniTakenToday` не персистятся.** После перезапуска в
   середине эксклюзивного квеста бот «забывает», что уже взял его. → Персистить, если
   это создаёт проблемы на практике.
6. **Много платных/долгих переездов за цикл.** Виноград + Статуя + Рыбалка + квесты —
   каждое отдельная поездка. По задумке ок, но на коротких follow-up-циклах это расход
   резерва/времени. → При желании развести тяжёлые под-задания по разным циклам.
7. **`PAUSE_SPEED_FACTOR = 0.5`** ускоряет все паузы вдвое — быстрее, но менее
   «человечно» (риск детекта). Осознанный компромисс.
8. **`manual_bot.js` пустой (0 байт)** и часть утилит — отладочные. → Удалить/убрать в
   отдельную папку, чтобы не засорять корень.

---

## 8. Быстрый старт (памятка)

```powershell
# 1. .env должен содержать TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID
# 2. Запуск менеджера:
node manager_bot.js
# 3. В Telegram: выбрать «Квесты» → «Запустить сейчас»
# 4. После правок кода — «Стоп», затем снова «Запустить сейчас»

# Проверка синтаксиса главного сценария без запуска:
node --check daily_quests_piraty.js
```

Тестов в проекте нет; проверка изменений — `node --check` + анализ реального лога запуска.
